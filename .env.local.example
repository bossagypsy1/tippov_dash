# Server-side only (no NEXT_PUBLIC_ prefix).
# Used by the Next.js proxy route at /app/api/proxy/[...path]/route.ts
# to forward browser requests to the external API without CORS issues.
API_BASE_URL=https://incident-management-api.azurewebsites.net/api
